<form method="post" action="{{ path('reservation_camp_delete', {'id': reservation_camp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_camp.id) }}">
    <button class="btn">Delete</button>
</form>
