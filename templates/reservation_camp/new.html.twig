{% extends 'base-front.html.twig' %}

{% block title %}New ReservationCamp{% endblock %}

{% block body %}
    <div class="card-body">

        <h4 style="color:#FFA500"  class="card-title">Ajouter reservation</h4>

        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
        {{form_widget(form)}}
        {{form_end(form)}}
    </div>

{% endblock %}
