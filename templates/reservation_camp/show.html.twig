{% extends 'base-back.html.twig' %}

{% block title %}ReservationCamp{% endblock %}

{% block body %}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 style="color:#FFA500"  class="card-title">  réservation</h4>


                    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_camp.id }}</td>
            </tr>
            <tr>
                <th>DateRes</th>
                <td>{{ reservation_camp.dateRes ? reservation_camp.dateRes|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Msg</th>
                <td>{{ reservation_camp.msg }}</td>
            </tr>
        </tbody>
    </table>

    <a  type="button" class="btn btn-warning" href="{{ path('reservation_camp_index') }}">liste des réservations</a>



                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
