{% extends 'base-back.html.twig' %}

{% block title %}Modifier évènement{% endblock %}
{% block body %}
    <div class="main-panel">
        <div class="content-wrapper">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">

                        <h4 style="color:#FFA500"  class="card-title">Modifier évènement</h4>
                        {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

                        {{form_start(form ,{'attr':{'class':'form-horizontal'}})}}
                        {{form_widget(form)}}
                        {{form_end(form)}}
                        <a href="{{ path('event_index') }}" type="button" class="btn btn-warning btn-fw">Liste évènements</a>

                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
