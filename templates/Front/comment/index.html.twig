{% extends 'baseBack.html.twig' %}

{% block title %}Voyage {% endblock %}

{% block body %}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 style="color:#FFA500"  class="card-title">liste Comments</h4>

                    <table class="table">
        <thead>
            <tr>


                <th>Comment</th>
                <th>User</th>
                <th>Action</th>




            </tr>
        </thead>
                        {% for item in voyages %}
        <tbody>

            <tr>
                <td>{{ item.content }}</td>

                  <td> {{ item.poster.email}} </td>
                <td>

                    <a href="{{ path('commentEdit', {'id': item.id}) }}">Modifier</a>
                    <a href="{{ path('commentDelete', {'id': item.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">Pas de voyage trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}


